# Miscellaneous
*.class
*.log
*.pyc
*.swp

# Chapter 4 thesis docs (local only, not for git)
docs/chapter4_evaluation_instrument.md
docs/chapter4_test_case_matrix.md
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# Claude Code local settings/permissions — machine- and session-specific,
# not something to share via version control.
.claude/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Vercel CLI's local project link (admin/ deploys via `vercel --prod` from
# the repo root — see .vercel/project.json) — machine-specific, not
# something to share via version control.
.vercel/

# Local scratch space for design-tool working files (Claude Design canvases,
# generated diagrams) — not part of the shipped app.
/scratch/
